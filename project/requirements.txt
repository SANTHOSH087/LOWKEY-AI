Flask==3.1.3
Flask-SQLAlchemy==3.1.1
Flask-Login==0.6.3
Flask-WTF==1.3.0
Werkzeug==3.1.3
email_validator==2.2.0
pytesseract==0.3.13
Pillow==12.1.1
reportlab==4.4.10
qrcode==8.2
pypdf==5.9.0
openpyxl==3.1.5

# --- security enhancements ---
Flask-Limiter==3.9.2     # login/register/reset/AI-chat rate limiting
Flask-Mail==0.10.0       # password reset + suspicious-login emails
pyotp==2.9.0             # TOTP-based 2FA (Google Authenticator / Authy compatible)
cryptography==43.0.3     # encrypted data exports (Fernet)
filetype==1.2.0          # real MIME/magic-byte validation for uploads, pure-python (no libmagic system dep)
clamd==1.0.2             # optional ClamAV malware scanning — only used if CLAMD_HOST is set; see DEPLOYMENT.md

# --- production deployment ---
gunicorn==23.0.0
psycopg2-binary==2.9.10   # Postgres driver — only needed if DATABASE_URL points at Postgres; SQLite needs nothing extra

# --- optional: only needed if you set OPENAI_API_KEY to enable LLM-enhanced
# assistant replies (see blueprints/assistant/llm.py — it lazy-imports this
# and falls back to the rule-based engine if it's missing) ---
# openai==1.54.0
